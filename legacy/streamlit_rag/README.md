# Legacy Streamlit RAG Prototype

This folder is the migration home for the original Streamlit, OpenSearch, and
SentenceTransformers prototype.

The current legacy source may still exist at the repository root during the
transition. New Echo work should use `packages/`, `apps/`, and `docs/` instead
of extending the legacy prototype.

